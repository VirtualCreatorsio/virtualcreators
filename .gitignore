# Ignore node modules (if using npm packages for build tools or APIs)
node_modules/

# Ignore build output folders (adjust if you use a build tool like Parcel, Vite, etc.)
dist/
build/
out/

# VS Code settings (local only)
.vscode/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS-generated files
.DS_Store
Thumbs.db

# Environment files (private keys, tokens, secrets)
.env
.env.local
.env.*

# Optional: Ignore form-handling service config (depending on platform)
netlify.toml
formspree.config.js
